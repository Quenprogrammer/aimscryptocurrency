import { AppTimeDisplayPipe } from './app-time-display.pipe';

describe('AppTimeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new AppTimeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
